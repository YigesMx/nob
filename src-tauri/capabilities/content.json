{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "content",
  "description": "Base capability for content window",
  "windows": ["content"],
  "permissions": [
    "core:default",
    "opener:default",
    "notification:default"
  ]
}
